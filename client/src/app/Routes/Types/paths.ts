export enum Paths {
    Home = "/",
    Account = "/my",
    AdminPanel = "/panel",
    Feedback = "/feedback",
    Catalog = "/catalog",
    CatalogComp = `/catalog/:type`,
    Product = `/catalog/:type/:id`,
}

export enum PathsAccount {
    Main = "/my/main",
    Cart = "/my/cart",
    Orders = "/my/orderlist",
    Favorites = "/my/favorites",
}
export enum PathsAdminPanel {
    Product = "/panel/product",
    Characteristic = "/panel/characteristic",
}


